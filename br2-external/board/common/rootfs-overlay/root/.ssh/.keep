# PlayOS Sprint 11.6 — /root/.ssh mount point for developer SSH.
# At runtime /data/ssh is bind-mounted over this directory so that
# authorized_keys persists on the USB data partition. This placeholder
# keeps the directory tracked in git and shipped by BR2_ROOTFS_OVERLAY.
