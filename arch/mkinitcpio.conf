# /etc/mkinitcpio.conf — PlayOS Arch initramfs
# Minimal hooks: GPU firmware + USB networking for PXE boot

MODULES=(amdgpu fat vfat)
BINARIES=()
FILES=()
HOOKS=(base udev autodetect modconf block filesystems keyboard)
