# PlayOS users table (Sprint 12)
#
#   username     uid   group         gid   password  home  shell  groups  comment
#
# playos-game is the unprivileged identity every game process runs as
# (S12-T1). UID/GID 1001 — kept in sync with playos-init's
# include/playos-init/security.h (PLAYOS_GAME_UID/GID). Supplementary
# groups: `audio` grants /dev/snd access so ALSA keeps working; `render`
# grants /dev/dri/renderD* access so the game's client-side EGL/GLES2
# path can reach the GPU through the Wayland seat; `input` grants
# /dev/input/event* access so libplayos's evdev backend can read the
# built-in controller. Games are intentionally NOT in video/drm — the
# primary node /dev/dri/card* stays root-only.
playos-game 1001 playos-game 1001 * - - audio,render,input PlayOS game user
