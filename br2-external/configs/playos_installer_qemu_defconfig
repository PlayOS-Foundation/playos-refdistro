# PlayOS QEMU x86_64 Installer Configuration (development variant)
#
# Same one-shot internal-disk installer state machine as the production
# playos_ally_installer_defconfig, but built for the QEMU/virtio-gpu dev
# target instead of physical ROG Ally hardware:
#   * generic x86_64 userspace (no -march=haswell, so it runs under TCG/KVM)
#   * software GL (softpipe) instead of radeonsi — QEMU has no AMD GPU
#   * virtio + virtio-gpu/bochs kernel drivers for the graphical path
#
# The headless loopback install test does NOT require this variant: it boots
# the production installer build with playos.install.auto=1. This variant is
# for exercising the graphical installer UI under QEMU.
#
# Generated: Sprint 10 — Installer bring-up (T8 QEMU validation)

# ── Architecture ──────────────────────────────────────────────────
BR2_x86_64=y
BR2_x86_64_generic=y

# ── System ────────────────────────────────────────────────────────
BR2_ROOTFS_DEVICE_CREATION_DYNAMIC_EUDEV=y
BR2_ROOTFS_DEVICE_TABLE=""
BR2_ROOTFS_POST_BUILD_SCRIPT=""
BR2_TARGET_GENERIC_HOSTNAME="playos-installer"
BR2_TARGET_GENERIC_ISSUE="PlayOS Installer (QEMU)"
BR2_TARGET_GENERIC_GETTY_PORT="tty1"
BR2_TARGET_GENERIC_GETTY_BAUDRATE_115200=y

# ── Kernel ────────────────────────────────────────────────────────
BR2_LINUX_KERNEL=y
BR2_LINUX_KERNEL_CUSTOM_VERSION=y
BR2_LINUX_KERNEL_CUSTOM_VERSION_VALUE="6.12.103"
BR2_LINUX_KERNEL_USE_CUSTOM_CONFIG=y
BR2_LINUX_KERNEL_CUSTOM_CONFIG_FILE="$(BR2_EXTERNAL_PlayOS_PATH)/board/qemu-installer/linux.config"
BR2_GLOBAL_PATCH_DIR="$(BR2_EXTERNAL_PlayOS_PATH)/board/patches"
BR2_LINUX_KERNEL_NEEDS_HOST_LIBELF=y
BR2_LINUX_KERNEL_INITRAMFS_SOURCE="$(BINARIES_DIR)/rootfs.cpio"

# ── Toolchain ─────────────────────────────────────────────────────
BR2_TOOLCHAIN_BUILDROOT_MUSL=y
BR2_TOOLCHAIN_BUILDROOT_CXX=y
BR2_KERNEL_HEADERS_6_12=y

# ── Initramfs ─────────────────────────────────────────────────────
BR2_TARGET_ROOTFS_INITRAMFS=y
BR2_TARGET_ROOTFS_INITRAMFS_CUSTOM=y
BR2_ROOTFS_OVERLAY="$(BR2_EXTERNAL_PlayOS_PATH)/board/common/rootfs-overlay"

# ── Filesystem images ─────────────────────────────────────────────
BR2_TARGET_ROOTFS_EXT2=y
BR2_TARGET_ROOTFS_EXT2_SIZE="512M"

# ── Bootloader (UEFI direct EFI stub, no GRUB) ───────────────────
# Kernel is built with CONFIG_EFI_STUB=y and placed directly as
# EFI/BOOT/BOOTX64.EFI on the ESP. No intermediate bootloader needed.

# ── Packages — Base system ────────────────────────────────────────
BR2_PACKAGE_BUSYBOX=y
BR2_PACKAGE_BUSYBOX_CONFIG="$(BR2_EXTERNAL_PlayOS_PATH)/board/common/busybox.config"
BR2_PACKAGE_EUDEV=y

# ── Packages — Firmware ───────────────────────────────────────────
# No AMDGPU firmware needed for QEMU (virtio-gpu/bochs are in-kernel).
# Keep the linux-firmware package for the base firmware blobs.

# ── Packages — Graphics stack ────────────────────────────────────
# softpipe is the software Gallium driver; virtio-gpu + bochs-drm provide
# the DRM/KMS device under QEMU. No LLVM dependency (unlike llvmpipe).
BR2_PACKAGE_MESA3D=y
BR2_PACKAGE_MESA3D_GALLIUM_DRIVER_SOFTPIPE=y
BR2_PACKAGE_MESA3D_OPENGL_EGL=y
BR2_PACKAGE_MESA3D_OPENGL_ES=y
BR2_PACKAGE_MESA3D_GBM=y

# ── Packages — PlayOS components ──────────────────────────────────
BR2_PACKAGE_PLAYOS_INIT=y
BR2_PACKAGE_PLAYOS_PLATFORM_API=y
BR2_PACKAGE_PLAYOS_RUNTIME=y
BR2_PACKAGE_PLAYOS_COMPOSITOR=y
BR2_PACKAGE_PLAYOS_SHELL=y
BR2_PACKAGE_PLAYOS_RAYLIB=y
BR2_PACKAGE_PLAYOS_OVERLAY=y
BR2_PACKAGE_PLAYOS_INSTALLER=y
BR2_PACKAGE_PLAYOS_SAMPLES=y

# ── Packages — Audio tools (for hw-check) ─────────────────────────
BR2_PACKAGE_ALSA_UTILS=y
BR2_PACKAGE_ALSA_UTILS_APLAY=y

# ── Packages — Hardware utilities ─────────────────────────────────
BR2_PACKAGE_EVTEST=y

# ── Packages — Installer tools ────────────────────────────────────
# libfdisk (GPT write), blockdev/mkfs/fdisk (partition sync + filesystems),
# and efibootmgr (best-effort NVRAM boot entry). e2fsprogs supplies mkfs.ext2/4.
BR2_PACKAGE_UTIL_LINUX=y
BR2_PACKAGE_UTIL_LINUX_BINARIES=y
BR2_PACKAGE_DOSFSTOOLS=y
BR2_PACKAGE_DOSFSTOOLS_MKFS_FAT=y
BR2_PACKAGE_E2FSPROGS=y
BR2_PACKAGE_EFIBOOTMGR=y

# ── Host utilities ────────────────────────────────────────────────
BR2_PACKAGE_HOST_DOSFSTOOLS=y
BR2_PACKAGE_HOST_GENIMAGE=y
BR2_PACKAGE_HOST_MTOOLS=y
